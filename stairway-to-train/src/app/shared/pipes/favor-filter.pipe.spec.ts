import { FavorFilterPipe } from './favor-filter.pipe';

describe('FavorFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FavorFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
