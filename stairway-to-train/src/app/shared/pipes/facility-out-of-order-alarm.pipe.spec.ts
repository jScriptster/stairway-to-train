import { FacilityOutOfOrderAlarmPipe } from './facility-out-of-order-alarm.pipe';

describe('FacilityOutOfOrderAlarmPipe', () => {
  it('create an instance', () => {
    const pipe = new FacilityOutOfOrderAlarmPipe();
    expect(pipe).toBeTruthy();
  });
});
