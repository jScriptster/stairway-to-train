import { StationPreviewPipe } from './station-preview.pipe';

describe('StationPreviewPipe', () => {
  it('create an instance', () => {
    const pipe = new StationPreviewPipe();
    expect(pipe).toBeTruthy();
  });
});
